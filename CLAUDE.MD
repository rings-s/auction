1. Add a TODO list before creating the code, as the code will be generated based on the listed features.
2. I am using Django 5.2 and django restApi for the backend and SvelteKit with Svelte 5 for the frontend.
3. Use top-tier UI/UX design principles, including:
    - Thin icons
    - Simple icons
    - Clean and simple animations
4. Maintain consistent code across all pages.
5. Use the app-level CSS styles for the core application.
6. Write the most simple and concise code to handle the frontend.
7. Base all frontend features strictly on the same model fields and serializers from the backend.
8. Do not change the backend code if it's correct.
9. Ensure that every code snippet generated is concise and renders the same exact backend features, with full alignment to the app context.
10. Follow the same analytics structure used across the application.
11. Avoid generating redundant code.
12. If there are two dashboards or similar pages, combine them into one using a clean, smart layout.
13. Ensure that the lib files, routes, and Django apps are already consistent and connected properly.
14. Revise the code multiple times before final generation.
15. Add console logs to help fix errors successfully.
16. Make sure the generated code supports both English and Arabic translations.
17. Avoid generating redundant components or pages.
18. Avoid creating multiple layouts — use the standard layout page only.